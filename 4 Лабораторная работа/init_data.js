const fs = require('fs');

const sql = [];

sql.push(`INSERT INTO position (id, name, description, duties) VALUES`);
sql.push(`    ('11111111-0000-0000-0000-000000000001', 'курьер',`);
sql.push(`     'Доставка заказов покупателям',`);
sql.push(`     'Забирает одежду со склада, звонит покупателю, доставляет по адресу, оформляет накладную'),`);
sql.push(`    ('11111111-0000-0000-0000-000000000002', 'продавец-консультант',`);
sql.push(`     'Помощь покупателям в торговом зале',`);
sql.push(`     'Консультирует покупателей, приносит одежду со склада, оформляет чеки и заказы'),`);
sql.push(`    ('11111111-0000-0000-0000-000000000003', 'администратор',`);
sql.push(`     'Управление магазином',`);
sql.push(`     'Подписывает трудовые договоры, контролирует работу сотрудников')`);
sql.push(`ON CONFLICT DO NOTHING;`);

fs.writeFileSync('init_data.sql', sql.join('\n'), 'utf8');

